The directories within this package:

* doc
  Documentation.
* etc
  A sample configuration file for libsensors.
* lib
  The user-space sensors support library code (libsensors).
* prog
  Several supporting programs. Includes programs for detecting installed
  hardware and recommending specific modules to install.
  Also includes programs to report sensor readings, for use
  after the package has been installed.
  Some of these use the sensors support library, some do not.
